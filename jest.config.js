module.exports = {
    testEnvironment: 'node', // Use the Node.js environment to run the tests
    roots: ['<rootDir>/tests'], // Look for tests in the 'tests' directory
    testMatch: ['**/*.test.js'] // Run files that end with '.test.js'
};